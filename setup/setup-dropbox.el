(setq dropbox-dir "~/Dropbox")

;; (when (file-directory-p dropbox-dir)
;;   (add-hook 'after-init-hook
;; 	    (lambda ()
;; 	      (switch-to-buffer (find-file "~/Dropbox/Org/Todo.org")))))

(provide 'setup-dropbox)
